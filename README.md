# Track2Art project page

Static project page for **Track2Art: Motion-Centric Articulated Object Model Recovery from 2D Point Trackers**.

GitHub Pages serves the repository root from `main`. For local preview:

```bash
python3 -m http.server 8797
```

Then open `http://127.0.0.1:8797/`.
